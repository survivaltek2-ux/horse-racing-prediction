# SSH Deployment Configuration - Test Environment
# This is a sample configuration for demonstration purposes

# Remote server configuration
REMOTE_HOST="test-server.example.com"
REMOTE_USER="deploy"
REMOTE_APP_DIR="/opt/horse-racing-app"

# SSH configuration
SSH_KEY_PATH="~/.ssh/id_rsa"

# Optional: Custom SSH port (default is 22)
SSH_PORT="22"

# Optional: Connection timeout in seconds (default is 30)
SSH_TIMEOUT="30"

# Optional: Backup existing deployment before transfer
BACKUP_EXISTING="true"

# Note: This is a test configuration
# Replace with your actual server details before deployment